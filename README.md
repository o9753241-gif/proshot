# ProShot для iOS

Порт `ai.proshot.app` в App Store. Клиент — нативный SwiftUI, сервер — отдельный
инстанс существующего FastAPI-бэкенда.

Исходники Android-версии лежат в соседней папке `ProShot` и здесь только читаются.

## Что где

```
server/     бэкенд iOS-инстанса (см. MERGE_REPORT.md — как он собран)
server/tests/  прогон развилки покупок без обращения к Apple
deploy/     systemd-юнит, файл Caddy, скрипт развёртывания
docs/       APPLE_BILLING.md — как устроены покупки и что настроить
ios/        проект Xcode — появится на этапе 5
```

## Развёртывание сервера

На VPS от root:

```
bash deploy/deploy.sh
```

Скрипт проверяет, что порт 8011 свободен, кладёт код в `/root/backend-ios`,
поднимает окружение, ставит юнит `proshot-ios` и файл в `/etc/caddy/conf.d/`.
Существующие инстансы не трогает: Android на 8010, Starshot на 8000, Nemi на 8012.

Перед первым запуском впиши в `/root/backend-ios/.env` ключ `DASHSCOPE_API_KEY`.

## Этапы

1. ~~Apple Developer Program~~ — оплачен, ждём подтверждения
2. **Свести две копии бэкенда** — сделано, см. `MERGE_REPORT.md`
3. Поднять iOS-инстанс на VPS — файлы готовы в `deploy/`, ждёт запуска
4. **Проверка покупок Apple** — сделано, см. `docs/APPLE_BILLING.md`
5. Xcode-проект (нужен Mac)
6. Товары и запись приложения в App Store Connect
7. Восемь экранов на SwiftUI
8. Приватность и метаданные
9. Песочница, TestFlight, подача

## Правила по проекту

Продуктовая логика, лимиты, тарифы и тексты не меняются без решения владельца.
Всё, что было изменено при сведении, перечислено в `MERGE_REPORT.md`.
